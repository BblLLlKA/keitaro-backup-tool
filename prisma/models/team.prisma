model Team {
  id        String    @id @default(uuid()) @db.Uuid

  name      String

  servers   Server[]

  deletedAt DateTime?
  
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  
  @@index([deletedAt])

  @@index([name])
  @@map("teams")
}
