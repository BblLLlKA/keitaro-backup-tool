model UserRole {
  id        String    @id @default(uuid()) @db.Uuid
  name      String    @unique
  users     User[]

  deletedAt DateTime?

  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt

  @@index([deletedAt])

  @@map("user_roles")
}
